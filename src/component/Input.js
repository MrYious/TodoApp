import { Grid } from "@mui/material";

export default function Input ({ tasks }){

    return <>
        <Grid 
            item
            container
        >
            {console.log(tasks)}
        </Grid>
    </>;
}